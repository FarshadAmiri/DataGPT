# راهنمای استفاده از گزارش فارسی DataGPT

## فایل‌های ایجاد شده

### 1. README_FA.md
فایل مارک‌داون فارسی که شامل گزارش کامل است. این فایل را می‌توانید:
- در GitHub یا GitLab نمایش دهید
- با ویرایشگرهای متن باز کنید
- به سایر فرمت‌ها تبدیل کنید

### 2. DataGPT_Report_Persian.docx
فایل Word با فرمت‌بندی حرفه‌ای که شامل:
- پشتیبانی کامل از راست‌چین (RTL)
- فونت فارسی B Nazanin
- سرفصل‌های رنگی
- بلاک‌های کد با قاب
- فرمت‌بندی بصری مناسب

### 3. generate_word_report.py
اسکریپت Python برای تبدیل خودکار README_FA.md به Word.

## محتویات گزارش

گزارش شامل بخش‌های زیر است:

### 📋 1. مقدمه
- معرفی DataGPT
- ویژگی‌های کلیدی
- بازیابی هوشمند
- هوشمندی پایگاه داده
- تحلیل صفحات گسترده
- امنیت و عملکرد
- شفافیت کوئری

### 🎯 2. موارد استفاده (Use Cases)
- تحلیل اسناد و مدارک
- تحلیل دیتابیس کسب‌وکار
- تحلیل گزارش‌های اکسل
- پشتیبانی مشتری
- تحقیقات علمی

با مثال‌های عملی برای هر سناریو

### 📖 3. راهنمای استفاده
- پیش‌نیازها و نصب
- راه‌اندازی سرور
- ایجاد Collection (سه نوع: Document, Database, Excel)
- ایجاد Thread
- چت با داده‌ها
- مدیریت دسترسی‌ها

با دستورات کامل و نمونه سوالات

### 🚀 4. راهنمای استقرار (Deployment)
سه روش کامل:

#### روش 1: استقرار روی سرور لینوکس
- نصب پیش‌نیازها
- تنظیمات Django
- راه‌اندازی Gunicorn با Systemd
- راه‌اندازی Daphne برای WebSocket
- پیکربندی Nginx
- راه‌اندازی vLLM
- راه‌اندازی SSL با Let's Encrypt

#### روش 2: استقرار با Docker
- Dockerfile
- docker-compose.yml
- دستورات اجرا

#### روش 3: استقرار روی Windows Server
- نصب پیش‌نیازها
- راه‌اندازی با Waitress
- ایجاد Windows Service

### 🔌 5. مستندات API
مستندات کامل شامل:

#### Endpoints:
- لیست و مشاهده Thread ها
- ایجاد Thread جدید
- لیست و مشاهده Collection ها
- ایجاد Collection (سه نوع)
- اضافه کردن اسناد
- حذف Thread/Collection
- بازسازی ایندکس
- دانلود فایل

#### WebSocket API:
- اتصال WebSocket
- فرمت پیام‌های ارسالی
- فرمت پیام‌های دریافتی
- مثال JavaScript

#### نمونه کدها:
- Python Client کامل
- مثال‌های cURL
- مثال‌های JavaScript

#### کدهای وضعیت و محدودیت‌ها

### 🏗️ 6. معماری فنی
- نمای کلی معماری
- Stack تکنولوژی
- مدل داده (Data Models)
- جریان کاری (Workflows)
- امنیت
- عملکرد و مقیاس‌پذیری
- نظارت و Logging

## نحوه استفاده

### برای نمایش آنلاین:
فایل `README_FA.md` را در مخزن Git خود قرار دهید.

### برای چاپ یا ارائه:
فایل `DataGPT_Report_Persian.docx` را باز کنید.

**نکته:** برای نمایش صحیح فارسی، فونت B Nazanin باید روی سیستم نصب باشد.
اگر فونت نصب نیست، از این لینک دانلود کنید:
https://fontdownload.ir/b-nazanin-font/

### ویرایش و سفارشی‌سازی:
1. فایل `README_FA.md` را ویرایش کنید
2. اسکریپت را اجرا کنید:
   ```
   python generate_word_report.py
   ```
3. فایل Word جدید ایجاد می‌شود

## ساختار فایل Word

فایل Word دارای فرمت‌بندی زیر است:

- **سرفصل‌ها:** رنگ آبی تیره، فونت B Nazanin، راست‌چین
- **متن عادی:** فونت B Nazanin 11pt، راست‌چین
- **کدها:** فونت Consolas/Courier New، چپ‌چین با قاب خاکستری
- **لیست‌ها:** Bullet points با فونت فارسی

## مشکلات احتمالی و راه‌حل

### مشکل: فونت فارسی نمایش داده نمی‌شود
**راه‌حل:** فونت B Nazanin را نصب کنید یا در اسکریپت `generate_word_report.py` فونت دیگری (مثل Tahoma) را انتخاب کنید.

### مشکل: راست‌چین نبودن متن
**راه‌حل:** مطمئن شوید که Word یا LibreOffice شما از RTL پشتیبانی می‌کند. در صورت لزوم، از Microsoft Word استفاده کنید.

### مشکل: کدها درست نمایش داده نمی‌شوند
**راه‌حل:** فونت‌های Monospace (Consolas, Courier New) باید روی سیستم نصب باشند.

## تولید مجدد گزارش

در صورت تغییر در پروژه، می‌توانید گزارش را به‌روزرسانی کنید:

1. محتویات `README_FA.md` را ویرایش کنید
2. اسکریپت را مجدد اجرا کنید:
   ```powershell
   D:/Projects/venv2/Scripts/python.exe generate_word_report.py
   ```

## اشتراک‌گذاری

- **فایل Markdown:** برای مخازن Git و نمایش آنلاین
- **فایل Word:** برای ارسال ایمیل، چاپ و ارائه‌های رسمی

## پشتیبانی

در صورت نیاز به تغییرات یا سفارشی‌سازی بیشتر در فرمت Word:
- فایل `generate_word_report.py` را ویرایش کنید
- تنظیمات فونت، رنگ، اندازه و فاصله‌ها را تغییر دهید
- توابع جدید برای فرمت‌بندی‌های پیشرفته‌تر اضافه کنید

---

**تاریخ ایجاد:** دی 1404  
**نسخه:** 1.0
