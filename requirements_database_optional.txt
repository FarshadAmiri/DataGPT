# Optional Database Driver Requirements

# Install based on your database needs:

# For MySQL support
pymysql>=1.1.0

# For PostgreSQL support  
psycopg2-binary>=2.9.9

# For MongoDB support
pymongo>=4.6.0

# For SQL Server support
pyodbc>=5.1.0

# Note: SQLite and Excel/CSV support work without additional packages
# (sqlite3 is built into Python, pandas is already in main requirements)
